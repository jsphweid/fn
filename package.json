{
  "name": "fn-monorepo",
  "workspaces": [
    "apps/*"
  ],
  "private": true,
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "grpc_tools_node_protoc_ts": "^5.2.2",
    "grpc-tools": "^1.11.1",
    "lerna": "~4.0.0",
    "prettier": "^2.3.0",
    "react": "~17.0.0",
    "ts-node": "^10.0.0",
    "typescript": "~4.2.0"
  },
  "engines": {
    "npm": ">=7"
  },
  "dependencies": {
    "graphql-codegen": "^0.4.0"
  }
}
